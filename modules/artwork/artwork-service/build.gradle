dependencies {

	compileOnly group: "javax.servlet", name: "javax.servlet-api"
	compileOnly group: "com.liferay.portal", name: "release.dxp.api"

	implementation project (':modules:utils')
	implementation project(":modules:artwork:artwork-api")
}

buildService {
	apiDir = "../artwork-api/src/main/java"
	osgiModule = true
	propsUtil = "eu.strasbourg.service.artwork.service.util.PropsUtil"
}