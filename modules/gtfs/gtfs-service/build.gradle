dependencies {

	compileOnly group: "javax.portlet", name: "portlet-api"
	compileOnly group: "javax.servlet", name: "javax.servlet-api"
	compileOnly group: 'javax.mail', name: 'mail', version: '1.4.7'
	compileOnly group: "com.liferay.portal", name: "release.dxp.api"

	compile project (":modules:gtfs:gtfs-api")
	compile project (":modules:utils")
}

buildService {
	apiDir = "../gtfs-api/src/main/java"
	osgiModule = true
	propsUtil = "eu.strasbourg.service.gtfs.service.util.PropsUtil"
}